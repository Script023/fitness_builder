package com.example.fitness_builder

import io.flutter.embedding.android.FlutterFragmentActivity

class MainActivity: FlutterFragmentActivity() {
    // This allows local_auth to hook into the Android fragment manager safely
}
