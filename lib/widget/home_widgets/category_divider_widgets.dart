import 'package:flutter/widgets.dart';

class CategoryDividerWidget extends StatelessWidget {
  const CategoryDividerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(width: 0.5, height: 48, color: const Color(0xFF896CFE)); //
  }
}